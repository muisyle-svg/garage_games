# Legacy scorekeeper snapshot

These files are preserved from the earlier Google Sheets sidebar scorekeeper.
They are archived for recovery and reference; the current event controller
uses `google-apps-script/` and `src/GarageGames.Controller/wwwroot/` instead.

The original files outside this repository remain untouched.
